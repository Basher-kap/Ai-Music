name: Create Preview Builds

#on:
#  push:
#    branches:
#      - 'preview/**'

jobs:
  build_android_preview:
    name: Build Android Preview
    type: build
    params:
      platform: android
      profile: preview